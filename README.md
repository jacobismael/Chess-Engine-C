# Chess Engine in C

Chess Engine written in C and CMake.

## Setup

Build the executable:
```bash
./setup.sh
```

or:
```bash
mkdir build
cd build
cmake ..
make
```

- - -

## Execution:

Visual Mode:
```bash
./main
```

Terminal Mode
```bash
cd build
./main <Name> <Location> <Output File>
```
